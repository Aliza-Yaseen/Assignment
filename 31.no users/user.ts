let user_name = ['aliza', 'sana', 'sania', 'sonya', 'mueza'];
if(user_name.length === 0){
    console.log('we need to find some users.');
}
else{
    user_name=[];
    console.log('all users have been removed. ' + user_name.length);
}
